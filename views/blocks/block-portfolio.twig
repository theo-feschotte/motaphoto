<section class="site-wrapper !pt-20 !pb-10 flex flex-col justify-center overflow-hidden" data-module-ajax data-ajax-action="portfolio" data-module-text>
    <div class="flex flex-col gap-10" data-speed="clamp(0.9)">
        <div class="flex gap-9">
            {% if fields.order_by_categories %}
                <div class="flex gap-2 items-center">
                    <label class="sr-only" for="categorie-select">Catégories</label>
                    <select id="categorie-select" name="categorie-select" class="w-64 p-5 pr-8 py-3 border-grey-default rounded-lg text-secondary text-xs font-medium uppercase tracking-[0.03em]" data-ajax="input">
                        <option value="">Catégories</option>
                        {% for categorie in categories %}
                            <option value="{{ categorie.slug }}">{{ categorie.title }}</option>
                        {% endfor %}
                    </select>
                </div>
            {% endif %}
            {% if fields.order_by_formats %}
                <div class="flex gap-2 items-center">
                    <label class="sr-only" for="format-select">Formats</label>
                    <select id="format-select" name="format-select" class="w-64 p-5 pr-8 py-3 border-grey-default rounded-lg text-secondary text-xs font-medium uppercase tracking-[0.03em]" data-ajax="input">
                        <option value="">Formats</option>
                        {% for format in formats %}
                            <option value="{{ format.slug }}">{{ format.title }}</option>
                        {% endfor %}
                    </select>
                </div>
            {% endif %}
            {% if fields.order_by_date %}
                <div class="ml-auto">
                    <select class="w-64 p-5 pr-8 py-3 border-grey-default rounded-lg text-secondary text-xs font-medium uppercase tracking-[0.03em]">
                        <option>Trier par</option>
                        <option value="desc">Récent au plus ancien</option>
                        <option value="asc">Ancien au plus récent</option>
                    </select>
                </div>
                {# <div class="ml-auto flex gap-2 items-center">
                    <label class="sr-only" for="orderby-select">Trier par</label>
                    <select id="orderby-select" name="orderby-select" class="w-64 p-5 pr-8 py-3 border-grey-default rounded-lg text-secondary text-xs font-medium uppercase tracking-[0.03em]" data-ajax="input">
                        <option>Trier par</option>
                        {% for order in orderby %}
                            <option value="{{ order.slug }}">{{ order.title }}</option>
                        {% endfor %}
                    </select>
                </div> #}
            {% endif %}
        </div>
        <div class="flex flex-col gap-10 items-center">
            {% include 'components/portfolio-results.twig' %}
            {% include 'components/pagination.twig' %}
        </div>
    </div>
</section>
