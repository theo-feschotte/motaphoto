<div class="w-full flex flex-col items-center gap-5 {{ posts.pagination.current == posts.pagination.total ? 'hiddenm' }}" data-ajax="pagination">
    <button class="btn {{ posts.pagination.current == posts.pagination.total ? 'bg-white hover:bg-white hover:text-black cursor-auto' }}" {{ posts.pagination.current == posts.pagination.total ? 'disabled' }} aria-label="{{ posts.pagination.current == posts.pagination.total ? 'Pas d\'autres photos à charger' : 'Charger plus de photos' }}" data-ajax="loadmore">{{ posts.pagination.current == posts.pagination.total ? 'Vous êtes à la fin.' : 'Charger plus' }}</button>
</div>